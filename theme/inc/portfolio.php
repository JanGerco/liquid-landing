<?php
/**
 * Portfolio items. Empty here: the section renders only when this list has entries.
 * Each item: name, slug (image at assets/img/portfolio/<slug>.jpg, 1317x875), dot colour, optional live url.
 */

defined( 'ABSPATH' ) || exit;

function ll_portfolio(): array {
	return [];
}
